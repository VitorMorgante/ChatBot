def retrieve_context(query, chatbot):
    """
    Recupera contexto relevante buscando na base vetorial (RAG).
    Na Fase 6, implementar busca por similaridade de cosseno (ex: pgvector).
    """
    # Retorna um contexto simulado para a arquitetura
    return f"Contexto simulado recuperado dos materiais vinculados ao chatbot '{chatbot.name}'."
